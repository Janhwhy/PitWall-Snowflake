.env
secrets/
cache/
__pycache__/
venv/
exports/

# Superseded by Snowflake (Cortex Search + structured tables) — no longer
# read by the API, keep locally only for the RAG_BACKEND=chromadb /
# DB_BACKEND=sqlite fallback paths.
vectorstore/
data/pitwall.db
backend_local.log

# Frontend
node_modules/
dist/
.DS_Store
.env.local
.env.*.local

